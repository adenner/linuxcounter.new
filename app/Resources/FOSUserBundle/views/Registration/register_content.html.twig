{% trans_default_domain "FOSUserBundle" %}
<p>
    We strongly believe in protecting the privacy of our subscribers. Any information provided to us will be held in strict confidence
    and will never be divulged, sold, or otherwise shared with any third parties.
    Please read our <a href="{{ url('syw_front_main_main_privacypolicy') }}">Privacy Policy</a> for more information.
</p>
<p>&nbsp;</p>
<form action="{{ path('fos_user_registration_register') }}" {{ form_enctype(form) }} method="POST" class="fos_user_registration_register">
    {{ form_widget(form) }}

    <div>
        <input type="hidden" name="fos_user_registration_form[locale]" value="{{ app.request.getLocale() }}" />
        <input type="submit" value="{{ 'registration.submit'|trans }}" />
    </div>
</form>
